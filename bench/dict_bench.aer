import time
start = time.now()
h = {}
i = 0
for i < 800000:
    h["key_{i}"] = i
    i += 1
sum = 0
i = 0
for i < 800000:
    sum += h["key_{i}"]
    i += 1
dur = time.now() - start
print("dict 200k insert+lookup: {dur}s, sum={sum}")
