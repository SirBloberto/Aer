import time
h = {}
i = 0
for i < 500:
    h["key_{i}"] = i * 3
    i += 1
start = time.now()
total = 0
n = 0
for n < 400000:
    total += h["key_{n % 500}"]
    n += 1
dur = time.now() - start
print("500-key table, 400k repeated lookups: {dur}s, total={total}")
