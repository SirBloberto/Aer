{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Compiler",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/binary/Compiler",
            "args": ["${workspaceFolder}/input/Input.txt"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build"
        }
    ]
}